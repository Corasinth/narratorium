<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/css/normalize.css">
    <link rel="stylesheet" href="/assets/css/skeleton.css">
        <link rel="stylesheet" href="/assets/css/style.css">
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <title>Narratorium</title>
</head>
<body>
    {{!-- Navbar -- need name on left and login/ signup on right --}}
    <div id="navbar">
        {{!-- <ul> --}}
        <a href="/"><h4>Narratorium</h4></a>
        <ul>
            {{!-- Links to login page, which offers a link to the signup page on it --}}
            {{#if loggedIn}}
            <h5 class="align-right" id="logout"><button>Logout</button></h5>
            {{else}}
            <h5 class="align-right"><a href="/login">Login</a></h5>
            <h5 class="align-right"><a href="/signup">Signup</a></h5>
            {{/if}}
        {{!-- </ul> --}}
    </div>
    {{{body}}}
    {{#if loggedIn}}
    <script src="/assets/js/logout.js"></script>
    {{/if}}
</body>
</html>